\documentclass[11pt]{article}
\usepackage[margin=1.1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs,array}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{setspace}
\usepackage{microtype}
\usepackage{csquotes}
\usepackage[backend=bibtex,style=authoryear,natbib=true,maxcitenames=2,maxbibnames=99]{biblatex}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}
\setstretch{1.15}
\captionsetup{font=small,labelfont=bf}

\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{assumption}{Assumption}
\newtheorem{remark}{Remark}
\theoremstyle{definition}
\newtheorem{definition}{Definition}

\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\Prob}{\mathbb{P}}
\newcommand{\one}{\mathbf{1}}
\DeclareMathOperator*{\argmax}{arg\,max}

\addbibresource{Ambiguity_Rollover_Crises_Paper.bib}
\graphicspath{{output/}}
\input{output/quant_numbers.tex}
\input{output/info_numbers.tex}

\title{Official Distrust, Investor Composition,\\ and Sovereign Rollover Crises}
\author{[Author]}
\date{July 2026}

\begin{document}
\maketitle

\begin{abstract}
\noindent The same fiscal report can support different rollover thresholds when different creditors hold the maturing debt. I study a global game in which creditor types differ in payoff hurdles and in effective pessimism about official information. A type-$k$ creditor lowers the public signal by $\delta_k$, which raises its run cutoff by $(\alpha/\beta)\delta_k$. Investor composition moves the unique crisis threshold through type-specific run propensities, amplified by the slope of the aggregate run map. Across 1,836 unique-equilibrium states, the coordination multiplier has median \MultMedian\ and a 10th to 90th percentile range from \MultPten\ to \MultPninety. Official precision stabilizes after favorable news and destabilizes after adverse news. Credibility lowers the threshold at every announcement. Max-min ambiguity and Bayesian pessimistic bias generate identical equilibrium choices and prices. The empirical design therefore measures effective pessimism with revision histories and investor surveys, estimates payoff hurdles from creditor behavior, and tests their interaction in spreads and auctions. The reference calculation produces spread increments of \PremCalm\ basis points in calm news and \PremStress\ basis points in stress.
\end{abstract}

\medskip
\noindent\textit{JEL codes}: D81, D82, F34, G01, H63.\\
\noindent\textit{Keywords}: rollover risk, global games, ambiguity, multiple priors, investor base, transparency.

\section{Introduction}\label{sec:intro}

In October 2009 the incoming Greek government announced that the budget deficit was far larger than previously reported. Subsequent releases raised the estimate again. The European Commission's January 2010 report documented serious weaknesses in Greek fiscal statistics \citep{EC2010Greece}. The episode changed the information problem facing a creditor. Creditors now priced an unknown bias alongside ordinary sampling error in each published fiscal number.

This paper studies that doubt and the investor base that carries it. Two sovereigns can publish the same fiscal report and face different rollover thresholds because their maturing bonds sit with creditors who have different incentives to run. Creditors face an unknown bias in official information and evaluate rollover under the worst prior in an interval, following \citet{GilboaSchmeidler1989}. Each type has its own rollover payoff, exit payoff, and effective pessimism. The investor base determines how much maturing debt each type controls.

The model has an exact as-if representation. Type $k$ behaves like a Bayesian creditor who lowers the official signal by $\delta_k$. Its private-signal cutoff rises by $(\alpha/\beta)\delta_k$. Distrust matters most where creditors place greater posterior weight on official information. The same representation proves that max-min ambiguity and Bayesian pessimistic bias produce identical market outcomes. The paper therefore centers its empirical analysis on effective pessimism and reserves the preference interpretation for evidence outside prices.

The global-game precision bound continues to select a unique equilibrium \citep{Hellwig2002,MorrisShin2003}. Distrust moves each type's cutoff without changing the slope bound. Investor composition then moves a unique threshold. The derivative is the difference between the types' run probabilities, multiplied by the coordination term $1/(1-\Phi')$. The same mechanism makes a given composition shift more costly when the run margin is densely populated.

Official precision and official credibility move different margins. Precision places more weight on the announcement. It lowers the crisis threshold when the announcement is sufficiently favorable and raises it after adverse news. Credibility reduces $\delta_k$ and lowers the threshold at every announcement. The model gives the exact news boundary at which the precision effect changes sign.

The reference calculation produces an effective-pessimism spread increment of \PremCalm\ basis points in calm news and \PremStress\ basis points in stress. Moving maturing debt toward the high-run-propensity type produces a \QTPremCalm\ basis point gap before adverse news and a \QTPremShock\ basis point gap after it. Raising recovery from 0.60 to 0.70 lowers the stress spread from \CACSBase\ to \CACSNew\ basis points. The multiplier distribution shows how these magnitudes change across the unique-equilibrium state space.

Global games supply the equilibrium machinery \citep{Hellwig2002,MorrisShin2003,MorrisShin2004}. Multiple-priors preferences follow \citet{GilboaSchmeidler1989}. \citet{Ui2025} shows that ambiguous-quality information can trigger a debt rollover crisis through strategic ambiguity. The present paper takes a different margin. It introduces heterogeneous effective pessimism and type-specific payoff hurdles, then lets investor composition move the aggregate run map. This structure yields exact composition derivatives, separates precision from credibility, and proves the observational equivalence that determines the empirical sequence. Sovereign rollover models provide the debt setting \citep{Calvo1988,ColeKehoe2000,BocolaDovis2019}. Holder accounts measure type weights while creditor behavior identifies their run propensities \citep{ArslanalpTsuda2014}.

The empirical sequence begins with information and creditor behavior. Real-time revision histories measure the objective error distribution. Investor surveys and release responses map that history into subjective $\delta_k$. A hierarchical forecast model separates private-signal noise from persistent model heterogeneity and rounding. Current holder accounts measure composition, while flows and auction participation identify type-specific payoff hurdles. Spreads and auction outcomes then test the predicted interaction. Institutional events provide descriptive validation, and the triple interaction supplies the core market test.

\section{The Rollover Game}\label{sec:model}

The analytical structure is exact inside the Gaussian one-shot game. Propositions~\ref{prop:premium} through \ref{prop:directional} follow from normal signals, max-min preferences over a bias interval, Assumption~\ref{ass:exit}, and the uniqueness inequality. Section~\ref{sec:quant} attaches magnitudes to those objects with documented payoffs, investor-base shares, information precisions, and revision-based distrust measures. Section~\ref{sec:policy} chains the static game into the policy experiments.

\subsection{Environment}

A sovereign has a unit continuum of short-term creditors, each holding one maturing bond. Each creditor chooses to roll over or to run. The sovereign survives the rollover iff the mass of runners is at most its liquid capacity $\theta$: fundamentals are the fraction of maturing debt the sovereign could redeem using buffers and primary surplus. If $\theta < 0$ the sovereign enters crisis regardless, and if $\theta > 1$ it survives even a full run. Between, survival is a coordination outcome.

Payoffs may differ across creditor types. Rolling over gives $R_k$ if the sovereign survives and $\mathcal R_k$ in crisis. Exiting gives $Q_k$ in survival and $q_k$ in crisis. Assume $R_k\geq\mathcal R_k$, $Q_k\geq q_k$, and
\begin{equation}
R_k-\mathcal R_k>Q_k-q_k,
\label{eq:relative-exposure}
\end{equation}
so rollover is more exposed to the crisis state than exit. Type $k$ rolls over when its assessed crisis probability satisfies
\begin{equation}
(1-P)R_k+P\mathcal R_k\geq(1-P)Q_k+Pq_k
\quad\Longleftrightarrow\quad
P\leq\bar p_k\equiv
\frac{R_k-Q_k}{(R_k-\mathcal R_k)-(Q_k-q_k)}.
\label{eq:pbar}
\end{equation}
The safe-exit benchmark sets $Q_k=q_k=1-\kappa_k$. The quantitative section further imposes common payoffs, giving $\bar p=\PbarPct$ percent.

Information has a public and a private layer. All creditors see the official signal $y = \theta + \sigma_y \eta$, precision $\alpha = \sigma_y^{-2}$: the budget documents and the program reviews. Each creditor $i$ also has private information $x_i = \theta + \sigma_x \varepsilon_i$, precision $\beta = \sigma_x^{-2}$, with $\varepsilon_i$ independent standard normal: his own analysis and client flows.

The equilibrium formulas use the diffuse-prior limit standard in the Gaussian global game. Conditional on $(x_i,y)$, posterior precision is $\alpha+\beta$. A sequence of proper normal priors with variance tending to infinity gives the same formulas. Section~\ref{sec:welfare} evaluates ex ante welfare under a proper objective distribution.

\subsection{Distrust as multiple priors}

The departure is that creditors discount the official signal. Type-$k$ creditors entertain the set of models in which the official signal carries an unknown bias $b$,
\[
y = \theta + b + \sigma_y \eta, \qquad b \in [-\delta_k,\, \delta_k],
\]
and evaluate actions by the worst model in the set \citep{GilboaSchmeidler1989}. The max-min creditor asks what rollover is worth when official information overstates capacity by the largest amount it regards as plausible. The radius $\delta_k$ measures this subjective effective pessimism. Historical revisions describe realized reporting errors. Investor surveys and behavior around releases determine which part of that error distribution enters the creditor's model set. Section~\ref{sec:evidence} estimates the mapping from revision histories and direct beliefs into $\delta_k$.

The relevant restriction compares the state exposure of rollover with the state exposure of exit.

\begin{assumption}[Common worst-case ordering]\label{ass:exit}
Conditional payoffs $(R_k,\mathcal R_k,Q_k,q_k)$ do not depend directly on the signal-bias model. Survival weakly raises the payoff to either action. Inequality~\eqref{eq:relative-exposure} holds and $0<\bar p_k<1$.
\end{assumption}

Both actions can now have state-dependent payoffs. A larger perceived crisis probability lowers the value of rollover more than the value of exit, so the same pessimistic model governs the comparison.

\begin{lemma}[Worst-case selection and as-if pessimism]\label{lem:asif}
Under Assumption~\ref{ass:exit}, the crisis event in a threshold equilibrium is $\{\theta\leq\theta^*\}$. The worst-case model for both actions is $b=\delta_k$. Type $k$ behaves as a Bayesian creditor who observed $y-\delta_k$ and uses the type-specific hurdle $\bar p_k$.
\end{lemma}

\begin{proposition}[Observational equivalence]\label{prop:equivalence}
Conditional on $(\delta_k,\bar p_k)$, creditor choices, the run mass, the crisis threshold, and model-implied spreads are identical under max-min ambiguity and under a Bayesian model in which type $k$ believes the official signal has pessimistic bias $\delta_k$. Market outcomes identify the common effective-pessimism representation. Evidence outside those outcomes identifies its preference source.
\end{proposition}

Revision histories establish the support of past reporting errors. Direct belief evidence identifies the admissible bias set before market outcomes evaluate the coordination mechanism. The model's distinct observable is the heterogeneous response to a common announcement after separate measurements identify payoff hurdles and prior bias beliefs.

\subsection{The effective-pessimism shift and uniqueness}

Bayesian updating from the effective signal gives type $k$'s posterior $\theta\mid x_i\sim N(m_k(x_i),(\alpha+\beta)^{-1})$. Threshold strategies are cutoffs $x_k^*$. The indifference condition $\Prob(\theta\leq\theta^*\mid x_k^*)=\bar p_k$ gives
\begin{equation}
x_k^*=x_{B,k}^*(\theta^*)+\frac{\alpha}{\beta}\delta_k,
\label{eq:cutoff}
\end{equation}
where $x_{B,k}^*$ contains the type-specific quantile $\Phi^{-1}(\bar p_k)$.

\begin{proposition}[The effective-pessimism shift and its loading]\label{prop:premium}
Type $k$'s run cutoff exceeds its Bayesian cutoff by $(\alpha/\beta)\delta_k$. This loading is independent of $\bar p_k$, though the level of the cutoff is not.
\end{proposition}

The loading $\alpha/\beta$ carries the economics. Distrust of the official source matters only insofar as the posterior leans on that source: where private information is abundant ($\beta$ large), creditors cross-check the government and $\delta$ is nearly free. Where the official account is all there is, every unit of doubt translates into cutoff. This is a cross-country prediction, stated as P2 in Section~\ref{sec:evidence}, and it inverts a comfortable intuition: the sovereigns most exposed to a credibility loss are precisely those whose information environment official sources dominate.

Aggregating, the mass of runners at fundamental $\theta$ is $\sum_k \mu_k \Phi(\sqrt{\beta}(x_k^* - \theta))$ for composition weights $\mu_k$, and the crisis threshold solves the fixed point
\begin{equation}
\theta^*=\sum_k\mu_k\Phi\!\left(g_k(\theta^*)+\frac{\alpha}{\sqrt\beta}\delta_k\right),
\qquad
g_k(\theta^*)\equiv
\frac{\alpha(\theta^*-y)-\Phi^{-1}(\bar p_k)\sqrt{\alpha+\beta}}{\sqrt\beta}.
\label{eq:fixedpoint}
\end{equation}

\begin{proposition}[Uniqueness is ambiguity-proof]\label{prop:unique}
If $\alpha/\sqrt{\beta} < \sqrt{2\pi}$, equation \eqref{eq:fixedpoint} has a unique solution, and the threshold equilibrium is the unique equilibrium in monotone strategies, for every composition $(\mu_k)$ and every profile of ambiguity radii $(\delta_k)$. Ambiguity shifts the map while leaving its slope controlled by the same precision ratio.
\end{proposition}

The condition is the familiar one \citep{Hellwig2002,MorrisShin2003}. Its economic content is independence from $\delta$. Distrust operates like a family of parallel signal translations, so heterogeneous radii preserve the slope condition. The reference precision ratio is \RatioVal\ against the bound \UniqBound. The multiplier $1/(1-\Phi')$ varies with the location of marginal creditors. Across the unique-equilibrium design distribution its median is \MultMedian, its interquartile range is \MultPtwentyfive\ to \MultPseventyfive, and its 90th percentile is \MultPninety.

On a twelve-cell sensitivity grid, private noise ranges from 0.10 to 0.30 and public noise from 0.25 to 0.50. The ratio $\alpha/\sqrt\beta$ runs from \RatioGridLo\ to \RatioGridHi, with \CellsNonUnique\ cells outside the sufficient uniqueness region. At baseline public noise, the frontier for private noise is $0.279$. The effective-pessimism stress increment exceeds the calm increment in every unique cell. Across the broader state design, the multiplier ranges from \MultPten\ at the 10th percentile to \MultPninety\ at the 90th percentile.

\begin{remark}[Polarization and source identification]\label{rem:polarization}
By Lemma~\ref{lem:asif}, one announcement $y$ enters type $k$'s posterior as $y-\delta_k$. Holding private signals fixed, posterior means differ by $\alpha/(\alpha+\beta)$ times the cross-sectional dispersion of $\delta_k$. Common signal noise leaves this cross-sectional wedge at zero. Heterogeneous Bayesian bias beliefs produce the same wedge, as Proposition~\ref{prop:equivalence} requires. Survey disagreement around releases therefore disciplines the distribution of effective pessimism. Preference evidence identifies its source.
\end{remark}

What the static game leaves open is who the types are and how many of each hold the debt. That composition is the paper's state variable, and the next section makes it the comparative-static of interest.

\section{The Investor Base}\label{sec:base}

For exposition, group creditors into low-run-propensity and high-run-propensity types, indexed by $S$ and $F$, with weights $1-\mu$ and $\mu$. Their payoff hurdles $\bar p_S$ and $\bar p_F$ may differ. The low type has effective pessimism $\delta_S$, while the high type has $\delta_F\geq\delta_S$. The quantitative benchmark sets common payoffs and $\delta_S=0$ to isolate the information margin. The empirical implementation estimates each sector's payoff hurdle from its funding structure, regulatory treatment, recovery exposure, and observed exit behavior. These estimated hurdles determine run propensity within holder sectors.

\begin{proposition}[Composition is priced, with amplification and interaction]\label{prop:base}
At the unique equilibrium of \eqref{eq:fixedpoint}, define $A_k=g_k+(\alpha/\sqrt\beta)\delta_k$. The composition derivative is
\[
\frac{d\theta^*}{d\mu}=\frac{\Phi(A_F)-\Phi(A_S)}{1-\Phi'}.
\]
Here $\phi_k=\phi(A_k)$ and $\Phi'=(\alpha/\sqrt\beta)\sum_k\mu_k\phi_k$.
It is positive if and only if the high type is more likely to run at the threshold. Holding payoff hurdles fixed, a rise in $\delta_F$ raises the crisis threshold. The size of either effect varies with the normal densities at the two run margins and with the coordination multiplier.
\end{proposition}

In the common-payoff reference calculation, effective pessimism raises the spread by \PremCalm\ basis points in calm news and \PremStress\ basis points in stress. Proposition~\ref{prop:equivalence} assigns these prices to the common equilibrium effect of max-min ambiguity and Bayesian pessimistic bias.

The numerator is the direct difference in run propensities. The equilibrium consequence is larger by $1/(1-\Phi')$. Forecast microdata discipline the direct response and the signal precisions. The fixed-point derivative then determines the implied amplification within the model.

\section{Official Information: Precision and Credibility}\label{sec:info}

An official communication policy in this model is a pair $(\alpha, \delta)$: how sharp the signal is, and how far it can be doubted. The pair moves the crisis threshold through different doors, and the distinction carries all of the section's content.

\begin{proposition}[Precision stabilizes conditionally, credibility unconditionally]\label{prop:transparency}
(i) The effect of official precision on the crisis threshold satisfies
\[
\mathrm{sign}\left(\frac{d\theta^*}{d\alpha}\right) \,=\, \mathrm{sign}\left(y^\dagger - y\right),
\qquad
y^\dagger
=\theta^*+
\frac{\sum_k\mu_k\phi_k\left[\delta_k-\Phi^{-1}(\bar p_k)/(2\sqrt{\alpha+\beta})\right]}
{\sum_k\mu_k\phi_k},
\]
so sharper official information lowers the crisis threshold if and only if $y>y^\dagger$. The hurdle exceeds $\theta^*$ when every type has $\bar p_k<1/2$. A rise in a type's distrust raises this hurdle in proportion to its mass and its density at the run margin. (ii) Credibility has an unambiguous threshold effect, $d\theta^*/d\delta_k=\mu_k\phi_k(\alpha/\sqrt\beta)/(1-\Phi')>0$, for every news level and every composition whenever $\alpha>0$.
\end{proposition}

Precision raises the weight placed on the announcement and on the common component of beliefs. It helps after sufficiently favorable news and hurts after sufficiently unfavorable news. Distrust raises the amount of good news required for precision to help. A reduction in $\delta_k$ lowers the threshold at every announcement. At the benchmark, halving the high type's radius lowers the stress spread by \HalfDeltaBp\ basis points.

\subsection{Ex ante welfare of official precision}\label{sec:welfare}

Let $L(\theta)>0$ denote the social loss from a rollover crisis and let $C(\alpha)$ denote the resource cost of producing a signal with precision $\alpha$. For a proper prior $F$ and the signal density generated by that prior, ex ante welfare is
\begin{equation}
W(\alpha)
=-\int L(\theta)\one\{\theta\leq\theta^*(y,\alpha)\}
f_\alpha(y\mid\theta)\,d y\,dF(\theta)-C(\alpha).
\label{eq:welfare}
\end{equation}
Equation~\eqref{eq:welfare} supplies the policy accounting boundary. A precision program raises welfare exactly when its integrated reduction in crisis losses exceeds its resource cost. A credibility reform lowers each $\delta_k$ and contracts the crisis region pointwise in the announcement. Its net benefit subtracts the reform cost. The conditional threshold result enters the ex ante policy calculation through this accounting \citep{AngeletosPavan2007}.

The next result separates distrust from a second-moment measure.

\begin{proposition}[Distrust moves direction, noise sets the anchor]\label{prop:directional}
(i) $d\theta^*/d\delta_k>0$ whenever type $k$ has positive mass and $\alpha>0$. (ii) As official precision vanishes, the threshold converges to
\[
\theta^*_\infty=\sum_k\mu_k(1-\bar p_k).
\]
The complete removal of public precision changes the threshold by $\theta^*_\infty-\theta^*$. It is destabilizing when the current threshold is below the anchor and stabilizing when the threshold is above it. (iii) In the latter states, the effect of removing public information and the effect of greater distrust have opposite signs. A variance measure is therefore not a global proxy for distrust.
\end{proposition}

Figure~\ref{fig:ambnoise} reports a finite scenario in which the official signal standard deviation rises by \DelF. The distrust shift raises the threshold throughout the plotted news range. The noise experiment changes sign because it moves the threshold toward the no-information anchor. This plotted comparison is a scenario result. The proposition signs the distrust derivative and the limit as public precision vanishes.

\section{Dynamics and Policy}\label{sec:policy}

The dynamic block links successive rollover games through composition, distrust, and fundamentals news. The calculations below change one slow state at a time.

\paragraph{Quantitative tightening as a composition treatment.} A central bank holds maturing debt with zero run margin: it is the limiting stable holder. Balance-sheet runoff hands each maturing bond back to the private margin, mechanically raising $\mu_t$. The experiment in Section~\ref{sec:quant} drifts $\mu$ from 0.35 to 0.65 over eight years, the order of magnitude of post-2022 runoff programs relative to maturing stocks, and prices the drift twice: against calm news throughout, and against a moderate deterioration arriving in year seven. The composition premium is \QTPremCalm\ basis points in the first case and \QTPremShock\ in the second. QT's rollover cost is a state-contingent exposure, essentially all of which is collected in bad states. A debt manager who evaluates runoff at calm-market spreads is marking an option position at its premium rather than its payoff.

\paragraph{The doom loop.} Higher spreads worsen future fundamentals through debt service, which feeds back into $y_{t+1}$. Appendix~\ref{app:dynamics} shows the feedback adds a genuinely dynamic instability: past a spread level, the stable fixed point disappears and the sovereign spirals, the \citet{Calvo1988} mechanism in modern dress, quantified for the Bayesian case by \citet{BocolaDovis2019}. The headline experiments set this feedback to zero to isolate the composition and credibility mechanisms. With a positive feedback, the same wedges shrink the distance between the calm equilibrium and the spiral's basin boundary.

\paragraph{Collective action clauses as coordination policy.} CACs and their statutory relatives raise expected recovery $\mathcal{R}$ in restructuring. Through \eqref{eq:pbar} they raise the hurdle $\bar p$, making creditors tolerant of more crisis risk before running, and through Proposition~\ref{prop:directional}(ii) they also lower the uninformative-signal anchor $1 - \bar p$. Both margins are quantified in Section~\ref{sec:quant}: recovery of 0.70 rather than 0.60 cuts the stress spread from \CACSBase\ to \CACSNew\ basis points, and shrinks the ambiguity premium itself, from 0.036 to 0.033 in threshold units, because bounding the loss bounds what the worst case can threaten. That last effect is the model's precise version of the proposal's conjecture that contractual design matters more in ambiguous environments: design that compresses worst-case payoffs is a substitute for trust.

\paragraph{Backstops as ambiguity floors.} An official facility that commits to lend against fundamentals above some floor truncates the support of the worst case: a creditor may doubt the budget numbers, while the facility itself anchors the lower tail. In multiple-priors terms the backstop cuts $\delta$ directly, which by Proposition~\ref{prop:transparency}(ii) stabilizes unconditionally, and its value is largest exactly when $\delta$ is high, a reading of catalytic finance \citep{MorrisShin2006} in which the catalyst works on the prior set rather than the signal. The corollary for program design is uncomfortable but useful: a backstop whose own activation is discretionary re-introduces the ambiguity it was meant to remove, and prices accordingly.

\section{Quantitative Magnitudes}\label{sec:quant}

\subsection{Calibration}

Table~\ref{tab:calib5} defines the reference calculation. Recovery of 0.60 follows the average haircut evidence in \citet{CrucesTrebesch2013}. The promised return and exit cost imply the rollover hurdle. Forecast microdata, release responses, and holder behavior supply the empirical targets for the information and investor-base block. The reference point satisfies the uniqueness condition. A broader design distribution spans 1,836 unique-equilibrium states and reports the full multiplier distribution.

\begin{table}[t]
\centering
\caption{Quantitative scenario.}
\label{tab:calib5}
\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabular}{lllll}
\toprule
Parameter & & Value & Category & Basis \\
\midrule
Rollover yield & $R$ & 1.04 & scenario & promised gross return \\
Recovery & $\mathcal{R}$ & 0.60 & external & haircut evidence \citep{CrucesTrebesch2013} \\
Exit cost & $\kappa$ & 0.08 & scenario & exit payoff \\
Hurdle & $\bar p$ & \PbarPct\% & internal & equation \eqref{eq:pbar} \\
Private noise & $\sigma_x$ & \SigX & scenario & forecast-microdata target \\
Official noise & $\sigma_y$ & \SigY & scenario & real-time-error target \\
Precision ratio & $\alpha/\sqrt{\beta}$ & \RatioVal & internal & uniqueness bound \UniqBound \\
Effective pessimism & $\delta$ & \DelF & scenario & revision history and belief response \\
High-run type weight & $\mu$ & \MuBase & scenario & holdings and estimated payoff hurdle \\
News, calm / stress & $y$ & 1.10 / 0.95 & scenario & capacity units \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Results}

\begin{table}[t]
\centering
\caption{Effective-pessimism-equivalent spread increments.}
\label{tab:results5}
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{Calm ($y = 1.10$)} & \multicolumn{2}{c}{Stress ($y = 0.95$)} \\
 & Bayesian & Distrust & Bayesian & Distrust \\
\midrule
Crisis threshold $\theta^*$ & \TCalmBay & \TCalm & 0.549 & \TStress \\
Crisis probability (\%) & 1.5 & \PCalmPct & 11.5 & \PStressPct \\
Spread (bp) & 61 & \SCalm & 477 & \SStress \\
Effective-pessimism increment (bp) & \multicolumn{2}{c}{\PremCalm} & \multicolumn{2}{c}{\PremStress} \\
\bottomrule
\end{tabular}
\par\smallskip
\begin{minipage}{0.85\textwidth}
\footnotesize Notes: $\mu=\MuBase$ and $\delta=\DelF$. \enquote{Bayesian} sets $\delta=0$. Proposition~\ref{prop:equivalence} gives the same differences under max-min ambiguity and Bayesian pessimistic-bias beliefs.
\end{minipage}
\end{table}

Table~\ref{tab:results5} reports the reference arithmetic. The same effective pessimism raises spreads by \PremCalm\ basis points in calm news and \PremStress\ basis points in stress because more creditors lie near the run margin after adverse news. The coordination multiplier varies across the state space. Its 10th percentile is \MultPten, its median is \MultMedian, its 90th percentile is \MultPninety, and the maximum unique-equilibrium value is \MultMaximum. Figure~\ref{fig:frontier} traces thresholds and stress spreads across investor composition. The remaining figures report the precision boundary, the distinction between distrust and noise, and the composition path.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{fig_frontier.pdf}
\caption{The composition frontier under stress news: crisis threshold (a) and spread (b) by the high-run-propensity type's weight, for effective-pessimism radii $\delta \in \{0.04,0.08,0.12\}$.}
\label{fig:frontier}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.6\textwidth]{fig_transparency.pdf}
\caption{The sign of $d\theta^*/d\alpha$: official precision stabilizes in the green region and destabilizes in the red, separated by the boundary $y^\dagger(\mu)$ of Proposition~\ref{prop:transparency}. Dots mark the calm and stress calibrations at $\mu = \MuBase$.}
\label{fig:transparency}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.6\textwidth]{fig_ambnoise.pdf}
\caption{Finite scenario comparison. The solid line gives the threshold effect of $\delta=\DelF$. The dashed line gives the effect of raising the official signal standard deviation by \DelF.}
\label{fig:ambnoise}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.62\textwidth]{fig_qt.pdf}
\caption{The state-contingent price of quantitative tightening: spread paths as the high-run-propensity weight moves from 0.35 to 0.65, against a fixed-weight counterfactual. A moderate fundamentals shock arrives in year seven.}
\label{fig:qt}
\end{figure}

\section{Empirical Designs}\label{sec:evidence}

The empirical work first measures official errors, creditor beliefs, and type-specific rollover incentives. Market outcomes then test the coordination mechanism in Proposition~\ref{prop:base}.

\paragraph{Revision histories and subjective pessimism.} Let $y_{cvt}$ be a real-time official forecast for country $c$, vintage $v$, and target date $t$. Put each forecast in liquid-capacity units using a predetermined mapping from debt service, fiscal balances, and reserves. The revision $e_{cvt}=y_{cvt}-y_{cFt}$ compares the release with the final vintage. Its upper quantiles describe realized overstatement. The following measurement equation maps that history and direct beliefs into $\delta_k$:
\begin{equation}
\delta_{kct}=h_k\!\left(Q_c(e),Z_{kct}\right),
\label{eq:delta-measurement}
\end{equation}
where $Z_{kct}$ contains direct survey beliefs and each creditor type's forecast response to official releases. The IMF historical WEO archive supplies public vintages \citep{IMFWEOData}. ECB SPF and Consensus forecast panels reveal how professional forecasters discount an official release after past revisions. The estimated $h_k$ maps objective error history into subjective effective pessimism.

\paragraph{Signal precision.} Forecast dispersion combines private information with persistent models, sticky updating, loss functions, and rounding. A hierarchical measurement model separates these components:
\begin{align}
f_{jcrh}&=x_{jcrh}+a_j+\ell_{jh}+m_{jc}+\zeta_{jc,r-1}+u_{jcrh}, \nonumber\\
x_{jcrh}&=\theta_{crh}+\varepsilon_{jcrh},
\qquad \varepsilon_{jcrh}\sim N(0,\sigma_x^2).
\label{eq:forecast-measurement}
\end{align}
Forecaster effects $a_j$, horizon effects $\ell_{jh}$, persistent model components $m_{jc}$, and lagged-information states $\zeta_{jc,r-1}$ absorb systematic disagreement. An interval-censored likelihood treats rounded reports. Repeated forecasts identify persistence separately from the transitory private signal. Real-time official forecast errors identify $\sigma_y^2$. ECB SPF microdata provide public individual forecasts \citep{ECBSPFData}, while licensed Consensus forecasts extend the country panel. Posterior draws for $\sigma_x^2$ and $\sigma_y^2$ enter the equilibrium solver jointly.

\paragraph{Investor composition and payoff hurdles.} Holder accounts reveal creditor sectors, while release behavior estimates run propensity. ECB Securities Holdings Statistics by Sector provide current euro-area weights $\mu_{kct}$ \citep{ECBSHSSData}. World Bank QPSD and QEDS provide current debt stocks and creditor splits \citep{WorldBankQPSD,WorldBankQEDS}. Sector flows around adverse releases, auction participation, funding maturity, and regulatory liquidity treatment identify $\bar p_k$. The empirical aggregate run index is
\begin{equation}
M_{ct}=\sum_k\mu_{kct}\Phi(\widehat A^{,0}_{kct}),
\label{eq:run-index}
\end{equation}
where $\widehat A^{,0}_{kct}$ sets effective pessimism to zero and retains the estimated payoff hurdle. This construction keeps the composition regressor separate from $D_{ct}$ and preserves continuous heterogeneity. The two-type quantitative figure projects this estimated distribution onto low and high run propensities. The historical panel of \citet{ArslanalpTsuda2014} covers its original sample period.

\paragraph{Market test.} With $(\delta,\alpha,\beta,M)$ measured before market outcomes, the core quarterly specification is
\begin{equation}
\Delta s_{i,t}=\alpha_i+\tau_t+\beta_1D_{i,t}+\beta_2M_{i,t}
+\beta_3\mathrm{Stress}_{i,t}+\beta_4D_{i,t}M_{i,t}
+\beta_5D_{i,t}M_{i,t}\mathrm{Stress}_{i,t}
+\Gamma'X_{i,t}+\varepsilon_{i,t},
\label{eq:panel5}
\end{equation}
where $D$ is the effective-pessimism estimate from \eqref{eq:delta-measurement} and stress uses information dated before the spread change. Proposition~\ref{prop:base} predicts $\beta_4>0$ when composition loads on higher run propensities. Density at the run margin predicts $\beta_5>0$. Forecast-variance posterior draws enter separately, preserving the distinction between pessimism and noise.

Primary auctions provide the quantity counterpart. Bid-to-cover, allotment tails, and cancellations are projected on the same predetermined interaction with issue fixed effects and narrow announcement windows. These outcomes reveal rollover pressure more directly than secondary-market spreads. Public release dates also permit tests of the sign boundary in Proposition~\ref{prop:transparency}.

Institutional reforms provide descriptive validation. Dissemination standards, fiscal councils, Article IV publications, and program reviews follow selected policy calendars. Their event windows show whether forecast responses and market sensitivity move in the direction of lower $\delta_k$. Equation~\eqref{eq:panel5}, estimated from predetermined information and holder states, remains the core test.

\begin{table}[t]
\centering
\caption{Predictions and implementation.}
\label{tab:evidence5}
\small
\begin{tabular}{p{7.2cm}p{3.2cm}p{1.8cm}p{1.9cm}}
\toprule
Prediction & Model magnitude & Null & Estimation \\
\midrule
Effective pessimism interacts with the run-propensity index & $\beta_4>0$ in eq.~\eqref{eq:panel5} & additive effects & spread panel \\
The cutoff response scales with official-information dependence & loading $\alpha/\beta$ & uniform & forecast microdata \\
Composition shifts raise sensitivity to adverse news & \QTPremCalm\ calm, \QTPremShock\ stress & state invariant & holder-sector panel \\
Precision changes sign at $y^\dagger$, credibility does not & Figure~\ref{fig:transparency} & uniform sign & release windows \\
Complete information loss converges to the weighted payoff anchor & Proposition~\ref{prop:directional} & current threshold & vintage comparisons \\
Effective pessimism widens forecast disagreement & Remark~\ref{rem:polarization} & common response & individual forecasts \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:evidence5} states model restrictions and the data moment that bears on each. Forecast polarization and release responses estimate effective pessimism. Choice evidence under known bias distributions determines whether that pessimism reflects max-min preferences or Bayesian beliefs.

\begin{table}[t]
\centering
\caption{Data and implementation.}
\label{tab:feas5}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{3.3cm}p{2.8cm}p{1.5cm}p{1.8cm}p{2.4cm}p{1.6cm}}
\toprule
Dataset & Coverage & Freq. & Access & Status & Use \\
\midrule
ECB SHSS by sector \citep{ECBSHSSData} & euro-area securities & quarterly & public aggregates & observed & current holder shares \\
World Bank QPSD and QEDS \citep{WorldBankQPSD,WorldBankQEDS} & reporting economies & quarterly & public & observed & current debt stocks \\
Historical investor base \citep{ArslanalpTsuda2014} & AE and EM sample & quarterly & public & observed & historical panel \\
Real-time official forecasts \citep{IMFWEOData} & WEO reporters & 2/y & public & observed & revision histories \\
Data-standard subscriptions & IMF dissemination standards & event & public & observed & event panel \\
ECB SPF microdata \citep{ECBSPFData} & euro area & quarterly & public & individual & precision moments \\
Consensus forecasts & country sample & monthly & licensed & individual & precision moments \\
Bond and CDS spreads & liquid sovereigns & daily & licensed & observed & licensed panel \\
Auction microdata & debt-office releases & per auction & uneven & observed where released & debt-office sample \\
Institutional and release dates & official archives & event & public & observed & timing tests \\
\bottomrule
\end{tabular}
\end{table}

\section{Conclusion}\label{sec:conclusion}

The investor base determines how official distrust enters a rollover crisis. Effective pessimism lowers type $k$'s public signal by $\delta_k$ and raises its run cutoff by $(\alpha/\beta)\delta_k$. Type-specific payoff hurdles locate marginal creditors. Their portfolio weights aggregate those margins into one crisis threshold, while $1/(1-\Phi')$ measures coordination amplification.

The same equilibrium follows from max-min ambiguity and Bayesian pessimistic bias. This equivalence organizes the evidence. Revision histories measure objective reporting errors. Creditor beliefs map those errors into $\delta_k$. Forecast panels identify information precision through the hierarchical measurement model. Holder accounts and observed exits identify the distribution of payoff hurdles. The triple interaction in equation~\eqref{eq:panel5} then tests the market mechanism.

Official precision changes sign with news, while credibility lowers the crisis threshold at every announcement. The reference spread increment rises from \PremCalm\ basis points in calm news to \PremStress\ basis points in stress. Across the unique-equilibrium state space, the multiplier has median \MultMedian\ and 90th percentile \MultPninety. These results show why a debt manager must price the creditor base together with the information environment.

\clearpage
\appendix
\renewcommand{\theequation}{\thesection.\arabic{equation}}
\numberwithin{equation}{section}

\section{Proofs}\label{app:proofs}

\subsection{Proof of Lemma \ref{lem:asif}}
Fix a profile of cutoff strategies. Its run mass is continuous and strictly decreasing in $\theta$, while liquid capacity is strictly increasing. There is one crossing $\theta^*$ and crisis occurs exactly when $\theta\leq\theta^*$. Under bias model $b$, let $P_b=\Prob_b(\theta\leq\theta^*\mid x_i,y)$. The posterior mean is $[\alpha(y-b)+\beta x_i]/(\alpha+\beta)$, so $P_b$ is increasing in $b$. The expected payoffs are
\[
U_k^R(b)=R_k-P_b(R_k-\mathcal R_k),
\qquad
U_k^E(b)=Q_k-P_b(Q_k-q_k).
\]
Assumption~\ref{ass:exit} makes both functions weakly decreasing in $P_b$. Their minima are therefore attained at $b=\delta_k$. Comparing the two minima gives
\[
U_k^R(\delta_k)-U_k^E(\delta_k)
=R_k-Q_k-P_{\delta_k}\big[(R_k-\mathcal R_k)-(Q_k-q_k)\big].
\]
The creditor rolls precisely when $P_{\delta_k}\leq\bar p_k$. This is the Bayesian decision computed from effective signal $y-\delta_k$. $\blacksquare$

\subsection{Proof of Proposition \ref{prop:equivalence}}
Lemma~\ref{lem:asif} maps every type's max-min problem into a Bayesian problem with the same payoff hurdle and the effective public signal $y-\delta_k$. Fix any cutoff profile. The mapping gives the same action for every private signal, hence the same aggregate run function. Its crossing with liquid capacity is the same. Applying the argument to the equilibrium cutoff profile proves equality of equilibrium choices and crisis probabilities. Any price that is a fixed function of the equilibrium crisis probability and state-contingent payoffs is also the same. $\blacksquare$

\subsection{Proof of Proposition \ref{prop:premium}}
With posterior $\theta\mid x\sim N(m_k(x),(\alpha+\beta)^{-1})$, indifference requires
\[
m_k(x_k^*)=\theta^*-\frac{\Phi^{-1}(\bar p_k)}{\sqrt{\alpha+\beta}}.
\]
Since $m_k(x)=[\alpha(y-\delta_k)+\beta x]/(\alpha+\beta)$, solving for the cutoff gives $x_k^*=x_{B,k}^*+(\alpha/\beta)\delta_k$, where $x_{B,k}^*$ is the cutoff at $\delta_k=0$ with the same payoff hurdle. Multiplication by $\sqrt\beta$ gives the shift $(\alpha/\sqrt\beta)\delta_k$ in the run index. $\blacksquare$

\subsection{Proof of Proposition \ref{prop:unique}}
Substituting the cutoffs into the crossing condition gives \eqref{eq:fixedpoint}. Its right side has derivative
\[
\frac{\alpha}{\sqrt\beta}\sum_k\mu_k\phi\left(g_k+\frac{\alpha}{\sqrt\beta}\delta_k\right)
\leq \frac{\alpha}{\sqrt{2\pi\beta}}<1.
\]
The bound is uniform in payoffs, composition, and distrust. The map sends $[0,1]$ into $(0,1)$ and is a contraction, so it has one fixed point. Each type's posterior crisis probability is strictly decreasing in its private signal, which gives one cutoff for that threshold. The resulting profile is the unique monotone equilibrium. $\blacksquare$

\subsection{Proof of Proposition \ref{prop:base}}
Let $s=\alpha/\sqrt\beta$, $A_F(\theta)=\Phi(g_F(\theta)+s\delta_F)$, and $A_S(\theta)=\Phi(g_S(\theta)+s\delta_S)$. With two types, the fixed point is
\[
H(\theta,\mu)=(1-\mu)A_S(\theta)+\mu A_F(\theta)-\theta=0.
\]
Write $\Phi'=s[(1-\mu)\phi_S+\mu\phi_F]$. The uniqueness condition gives $H_\theta=\Phi'-1<0$. Implicit differentiation gives
\[
\frac{d\theta^*}{d\mu}
=-\frac{H_\mu}{H_\theta}
=\frac{A_F(\theta^*)-A_S(\theta^*)}{1-\Phi'}.
\]
Its sign is the sign of the difference in the two run probabilities. Holding $\bar p_F$ fixed, differentiation with respect to $\delta_F$ gives
\[
\frac{d\theta^*}{d\delta_F}
=\frac{\mu s\phi_F}{1-\Phi'}>0.
\]
The numerator densities locate the marginal creditors. The denominator is the coordination multiplier. Both vary with the state. $\blacksquare$

\subsection{Proof of Proposition \ref{prop:transparency}}
Differentiate \eqref{eq:fixedpoint} in $\alpha$. Holding $\theta^*$ fixed gives
\[
\frac{\partial}{\partial\alpha}\left(g_k+\frac{\alpha}{\sqrt\beta}\delta_k\right)
=\frac{(\theta^*-y)-\Phi^{-1}(\bar p_k)/(2\sqrt{\alpha+\beta})+\delta_k}{\sqrt\beta}.
\]
Implicit differentiation therefore gives
\[
\frac{d\theta^*}{d\alpha}
=\frac{\sum_k\mu_k\phi_k\left[(\theta^*-y)-\Phi^{-1}(\bar p_k)/(2\sqrt{\alpha+\beta})+\delta_k\right]}{\sqrt\beta(1-\Phi')},
\]
whose sign is that of $y^\dagger-y$. If every $\bar p_k<1/2$, every quantile term is positive. The distrust terms are nonnegative. Hence $y^\dagger>\theta^*$. Direct differentiation in $\delta_k$ gives part (ii). $\blacksquare$

\subsection{Proof of Proposition \ref{prop:directional}}
Part (i) follows from direct differentiation of \eqref{eq:fixedpoint}. As $\alpha\to0$, $g_k\to-\Phi^{-1}(\bar p_k)$ and the distrust shifts vanish. The right side converges uniformly on $[0,1]$ to
\[
\sum_k\mu_k\Phi[-\Phi^{-1}(\bar p_k)]
=\sum_k\mu_k(1-\bar p_k).
\]
Uniform convergence and uniqueness imply convergence of the fixed point to this constant. Subtracting the current threshold gives the finite effect of removing public precision. Part (iii) compares that signed difference with the strictly positive derivative in part (i). $\blacksquare$

\section{Derivations: Pricing and Pass-Through}\label{app:pricing}

\paragraph{Spreads.} With risk-neutral pricing at a zero reference rate, a bond promising $R$ with crisis probability $P$ and recovery $\mathcal{R}$ carries spread $s = P(1-\mathcal{R})/[1 - P(1-\mathcal{R})]$ per period, the mapping used throughout. For small $P$ it is $P \times$ loss given default. Endogenizing $R$ through an issuance condition adds a second fixed point that raises all fragility statics (a higher threshold raises the promised yield, which through $\bar p$ raises the threshold again). The reported magnitudes are conservative in omitting it.

\paragraph{News pass-through and the multiplier.} Differentiating \eqref{eq:fixedpoint} in $y$ gives $d\theta^*/dy = -\Phi'/(1-\Phi')$ and hence
\[
\frac{dP}{dy} = \phi\big((\theta^*-y)\sqrt\alpha\big)\sqrt\alpha \left(\frac{d\theta^*}{dy} - 1\right) = -\,\frac{\phi\big((\theta^*-y)\sqrt\alpha\big)\sqrt\alpha}{1 - \Phi'}\,:
\]
the coordination multiplier scales the pass-through of public news to crisis risk. Forecast moments discipline the direct density and fixed-point slope before the empirical implementation maps this expression into market data.

\section{Dynamics and the Doom Loop}\label{app:dynamics}

Composition evolves by accounting: with runoff fraction $q_t$ of the official portfolio's maturing stock, $\mu_{t+1} = \mu_t + q_t(1 - \mu_t)\omega_t$, where $\omega_t$ is the official share of maturing debt. The QT experiment in the text uses a linear drift matching an eight-year normalization. The debt-service feedback closes fundamentals: $y_{t+1} = \bar y_{t+1} - \varsigma\,(s_t - s^{ref})$, service costs subtracting from next period's capacity news. Combining with the pass-through result above, the loop is locally explosive when
\[
\varsigma \times \frac{\phi\big((\theta^*-y)\sqrt\alpha\big)\sqrt\alpha\,(1-\mathcal{R})}{1 - \Phi'} \,>\, 1,
\]
a condition that tightens mechanically as stress raises the density term and as composition and distrust raise $\Phi'$: the same statics that price spreads shrink the distance to the spiral. This is the \citet{Calvo1988} instability with the coordination multiplier made explicit. \citet{BocolaDovis2019} quantify the Bayesian analogue. The text's experiments hold $\varsigma = 0$ to isolate the state-contingent spread gap that any positive $\varsigma$ would amplify.

\section{Parameter Discipline and Sensitivity}\label{app:params}

Table~\ref{tab:params5} records where each parameter enters, its units, its source, the range examined, and what moves. At $\delta = 0$ the model is the standard Bayesian global game and the distinctive predictions P1, P4's distrust term, P5, and P6 return to zero. As $\alpha/\sqrt\beta$ approaches \UniqBound, the multiplier grows and the distance to selection becomes the relevant statistic, with the margin discussed in Section~\ref{sec:model}.

\begin{table}[h]
\centering
\caption{Parameter discipline and sensitivity.}
\label{tab:params5}
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{p{2.0cm}p{1.9cm}p{1.8cm}p{2.8cm}p{1.9cm}p{3.0cm}}
\toprule
Parameter & Enters at & Units & Source or moment & Range examined & Effect of the range \\
\midrule
$R$, $\kappa$ & eq.~\eqref{eq:pbar} & face value & gross yield, fire-sale discount & joint shift & shift $\bar p$ jointly \\
$\mathcal{R}$ = 0.60 & eq.~\eqref{eq:pbar} & recovery rate & \citet{CrucesTrebesch2013} & 0.60 to 0.70 (CAC exp.) & stress spread \CACSBase\ to \CACSNew\ bp \\
$\bar p$ = \PbarPct\% & eq.~\eqref{eq:fixedpoint} & probability & implied & follows $\mathcal{R}$ & anchor $1 - \bar p$ moves \\
$\sigma_x$, $\sigma_y$ & eqs.~\eqref{eq:fixedpoint}, \eqref{eq:forecast-measurement} & capacity units & hierarchical forecast model & $\sigma_x$ 0.10 to 0.30, $\sigma_y$ 0.25 to 0.50 & multiplier p10 \MultPten, median \MultMedian, p90 \MultPninety \\
$\alpha/\sqrt\beta$ = \RatioVal & uniqueness & unitless & implied & up to bound \UniqBound & multiplier grows, then multiplicity \\
$\delta$ = \DelF & eqs.~\eqref{eq:cutoff}, \eqref{eq:delta-measurement} & capacity units & error history and belief response & 0.04 to 0.12 & spread increment \PremCalm\ to \PremStress\ bp \\
$\mu_k$, $\bar p_k$ & eqs.~\eqref{eq:fixedpoint}, \eqref{eq:run-index} & debt share, probability & holder accounts and observed exits & full type distribution & QT gap \QTPremCalm\ to \QTPremShock\ bp \\
$y$ calm/stress & eq.~\eqref{eq:fixedpoint} & fundamentals units & internal, calm spread \SCalm\ bp & full axis in figures & state dependence of every result \\
\bottomrule
\end{tabular}
\par\smallskip
\begin{minipage}{0.95\textwidth}
\footnotesize Notes: recovery follows external haircut evidence. The reference point sits inside the ranges shown. Section~\ref{sec:evidence} defines the measurement equations for the information and holder blocks.
\end{minipage}
\end{table}

\section{Data Appendix}\label{app:data}

\paragraph{Investor base.} ECB Securities Holdings Statistics by Sector supply current euro-area holder weights \citep{ECBSHSSData}. World Bank QPSD provides current public debt stocks \citep{WorldBankQPSD}. QEDS provides external debt statistics for reporting economies \citep{WorldBankQEDS}. The data of \citet{ArslanalpTsuda2014} supply their historical panel. Debt-office maturity schedules align holdings with the rollover date. Sector flows and auction participation estimate payoff hurdles, producing the continuous run index in \eqref{eq:run-index}.

\paragraph{Effective pessimism.} The IMF historical WEO archive supplies the public vintage panel \citep{IMFWEOData}. Revision quantiles measure realized reporting errors. Equation~\eqref{eq:delta-measurement} combines them with forecast responses and direct belief evidence to estimate $\delta_k$. Data-standard subscriptions \citep{Cady2004} and documented statistical-integrity events validate the direction of the estimated mapping.

\paragraph{Information precision.} ECB SPF individual forecasts provide public microdata for euro-area expectations \citep{ECBSPFData}. Licensed Consensus forecasts extend the country panel. Repeated observations identify persistent models and sticky updating in \eqref{eq:forecast-measurement}. Interval censoring absorbs strategic rounding. The remaining transitory component identifies private-signal variance after every outcome enters common capacity units.

\paragraph{Market outcomes.} Sovereign bond and CDS spreads at daily frequency for announcement windows and quarterly for the panel. Primary-auction results (bid-to-cover, stop-out tails, cancellations) from debt-management offices, and IMF Article IV and program-review publication dates from the IMF archive.

\section{Extensions}\label{app:extensions}

\paragraph{Heterogeneous private precision.} If type $k$ has private precision $\beta_k$, its cutoff shift becomes $(\alpha/\beta_k)\delta_k$ and its run index has slope $\alpha/\sqrt{\beta_k}$. A sufficient uniqueness condition is $\sum_k\mu_k\alpha/\sqrt{2\pi\beta_k}<1$. Forecast microdata can discipline the sector-specific variances. The composition derivative retains the difference in type-specific run probabilities divided by the derivative of the aggregate fixed-point map.

\paragraph{Smooth ambiguity.} Replacing max-min with smooth ambiguity aversion turns the worst-case shift $\delta_k$ into an endogenous pessimism weight varying with stakes. The as-if structure survives to first order, with $\delta_k$ reinterpreted as the local ambiguity premium. The max-min case is the tractable bound.

\paragraph{Learning about bias.} Letting creditors update the bias set from realized revisions makes $\delta_t$ a state with its own persistence: credibility as reputational capital. The asymmetry between slow accumulation and one-announcement destruction suggests trap dynamics complementary to those studied here.

\paragraph{Dynamic runs.} Staggered maturities in the sense of \citet{HeXiong2012} let today's rollover outcome inform tomorrow's, adding an intertemporal coordination motive to the static one. The composition state $\mu_t$ then also indexes the speed at which a run can build. The static threshold is the natural building block for that model.

\clearpage
\printbibliography

\end{document}
